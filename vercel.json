{
  "framework": "python",
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": "api",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.py" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}